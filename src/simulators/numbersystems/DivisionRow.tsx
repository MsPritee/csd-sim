import type { DivisionStep } from '../../core/numbersystems/types'
interface DivisionRowProps { readonly step: DivisionStep; readonly isActive: boolean; readonly isCompleted: boolean; readonly isSelected: boolean; readonly onSelect: () => void; readonly targetBase: 2 | 8 | 16 }
export function DivisionRow({ step, isActive, isCompleted, isSelected, onSelect, targetBase }: DivisionRowProps) { const remainder = targetBase === 16 && step.remainder >= 10 ? step.remainder.toString(16).toUpperCase() : String(step.remainder); return <button type="button" onClick={onSelect} className={`dv-row ${isActive ? 'is-active' : ''} ${isCompleted ? 'is-completed' : ''} ${isSelected ? 'is-selected' : ''}`} aria-label={`Division step ${step.stepNumber}: ${step.dividend} divided by ${step.divisor} equals ${step.quotient} with remainder ${remainder}`}><span>{step.divisor}</span><strong>{step.dividend}</strong><i>→</i><strong>{step.quotient}</strong><em className={step.remainder ? 'one' : 'zero'}>{remainder}</em></button> }
