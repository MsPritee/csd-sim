import type { Method } from './DefineFunctionPanel'

export const METHOD_LABELS: Record<Method, string> = {
  minterms: 'Minterms (∑m)',
  maxterms: 'Maxterms (∏M)',
  expression: 'Boolean Expression',
  truth: 'Truth Table',
}