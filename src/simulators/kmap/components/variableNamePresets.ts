/**
 * Standard, commonly-used variable names offered in the variable-name editor.
 * Kept in its own module so it can be shared without disabling fast-refresh
 * on the component file.
 */
export const PRESET_VARIABLE_NAMES = [
  'A', 'B', 'C', 'D', 'E',
  'W', 'X', 'Y', 'Z',
  'P', 'Q', 'R', 'S',
  'M', 'N', 'L', 'K',
] as const